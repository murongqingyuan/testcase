<view class="main">
  <v-toast isShow="{{toastIsShow}}" txt="{{toastContent}}" src="{{toastSrc}}"></v-toast>
  <image wx:if="{{showScrollTop}}" class="scrollTop" src="../../../images/icon_scrollTop.png" mode="aspectFit" bindtap="scrollToTop"></image>
  <view>
    <searchBox bind:add="addBill" bind:search="search" bind:reset="reset"></searchBox>
  </view>

  <view style="height:10rpx;">
    <!-- 防止margin-top 垂直方向塌陷，在第一个元素前增加空元素。 -->
  </view>
  <!-- 页脚 -->
  <view class="app_pageItem">
    <view class="app_text">已加载 {{travelPayBillList.length}}/{{totalCount}} 条
    </view>
    <view class="app_text" bindtap="scrollTop">返回顶部</view>
  </view>

  <scroll-view scroll-y="{{true}}" style="height:{{windowHeight-220}}rpx;margin-top: 160rpx;" refresher-enabled="true" bindrefresherrefresh="bindRefresh" refresher-triggered="{{isRefresh}}" bindscrolltolower="bindLoadMore" lower-threshold="50" scroll-top='{{scrollTopNum}}'
    scroll-with-animation="false">
    <loading position="center" wx:if="{{!inited}}"></loading>

    <payBillCard wx:for="{{travelPayBillList}}" wx:key="index" editable="{{true}}" payBillInfo="{{item}}" cardFrom="travel" vIndex="{{index}}"></payBillCard>
    <loading position="bottom" wx:if="{{loadMore}}"></loading>
  </scroll-view>
</view>